This is the final project for OOP.This is the final project for OOP.

## Project Overview
This repository is for our Final Term Project of Object-Oriented Programming (OOP) course at The Islamia University of Bahawalpur (IUB) class of 2015. For in-depth Object-Oriented Design (OOD) analysis, we have chosen the library called `rich`.For an in-depth Object-Oriented Design (OOD) analysis, we have chosen the library named `rich`. 

The `rich` library is a popular library for rich text and beautiful formatting in the terminal. We selected it because it is a well-known, modern code base and is heavily based on advanced OOP concepts such as inheritance, abstraction and polymorphism.

## Group Members
* Mohsin
* Qazi Reyan
* Rehan Bukhari

## Repository Structure
The file `/report` is our final written report in pdf format on the OOP analysis.
It includes our own custom Python extension which extends a class in the rich library.
The UML class hierarchy diagram is in the file `/diagrams`.

To run the code, simply carry out the steps below:
2. Run the commands to install the library: `pip install rich`
3. Go to the /code directory.
4. Assuming a successful run, the extension should appear in the game's menu.